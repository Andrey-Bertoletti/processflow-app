{
  "name": "processflow-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "front": "cd frontend && npm run dev",
    "back": "cd backend && supabase functions serve",
    "back:deploy": "cd backend && supabase functions deploy",
    "db:sync": "node scripts/sync-supabase-migrations.mjs",
    "banco": "npm run db:sync && cd backend && supabase db push",
    "banco:reset": "npm run db:sync && cd backend && supabase db reset",
    "supabase:push": "npm run db:sync && cd backend && supabase db push",
    "supabase:reset": "npm run db:sync && cd backend && supabase db reset",
    "supabase:start": "npm run db:sync && cd backend && supabase start",
    "supabase:stop": "cd backend && supabase stop",
    "test:scale": "node scripts/load-test/simulate-scale.mjs full-suite",
    "ops": "echo 'Dashboard em: http://localhost:3000/admin/system-health'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "vitest": "^4.1.5"
  }
}
